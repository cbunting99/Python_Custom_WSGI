# Core dependencies (pinned for reproducible installs)
httptools==0.6.0
hpack==4.0.0
setproctitle==1.3.2

# Platform-specific optimizations
uvloop==0.19.0; sys_platform != 'win32'

# Security dependencies
cryptography==41.0.0

# Optional dependencies for enhanced performance
cython==3.0.0

# Development / test dependencies
pytest==8.4.1
pytest-asyncio==0.23.0
pytest-cov==4.1.0
aiohttp==3.9.0
black==24.3.0
mypy==1.3.0

# Additional pinned packages to remediate known vulnerabilities (recommended)
requests==2.32.4
starlette==0.47.2
transformers==4.53.0
uv==0.8.6
